Nice work!

The marks are working GREAT, and very useful.


Notes:


--The edit time on long files (like fade out of a small portion) does seem to be slightly faster than it used to be, but it's still slower than the same operation on a short file.  Not sure what's going on here, so take a look.


--And YES, all operations (including copy-paste, delete, etc) should be the same speed regardless of file length.  Actually, I'm not sure this is possible, because you do have to shift more data (potentially).  But the wave-form calculation shouldn't be the slow part.  For example, pasting 1 second of audio at the end of a file should be the same speed, regardless of file size.  And it's currently slow.  I get that pasting audio at the beginning of a long file might be inherently slow... though it should be as fast as the memmove, right?  Let's see how fast that can be.  If that's still slow, we can talk later about ways to speed that up.


--There need to be a "jump to end" button, just like there's currently a jump to start.  It's very hard to move the cursor to the end of the file (without setting a mark there first with a selection, or zooming way out first).


--The selected region in the overview scroll area is too hard to see... I think that's because it doesn't have end-lines around it, and it's true that end-lines would make it more cluttered.  So make it a little bit more solid of a yellow than the selection in the main view, or something...  It's actually a bit dimmer than the yellow used in the main view, currently.


--Can you have a set of nice, distinct colors, and then cycle through those when setting marks, so that each pair of marks (or each lone mark) has a different color?  Would help us to remember which marks belong together.  Maybe like.. 8 distinct colors?


--There should be some small representation of the marks in the scroll overview... maybe just as tiny carets only.


--If we're currently playing audio (like looping a selection), then audio should continue playing after an operation.  Currently it stops.  Maybe it needs to stop during the operation, and then resume when done, but it would be great if it just kept playing the whole time.







