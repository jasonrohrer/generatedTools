cstatic: logging this report to /home/jasonrohrer/.claude/cstatic/cstatic-buggy-20260803-093702.log
cstatic v1.0 -- 1 source file(s) in /home/jasonrohrer/checkout/generatedTools/cstatic/tests/buggy
cstatic: -f world.c -- isolation mode: each file is judged on its own, with no idea who calls it
cstatic: only that file and the headers it includes are read; the rest of the folder is not looked at
cstatic: model=opus effort=high jobs=4  (1 analysis pass(es))

cstatic: [1/1] read world.c -- 8 candidate(s)

cstatic: 8 candidate finding(s) after dedupe
cstatic: verifying (each candidate gets a fresh, skeptical reader)

cstatic: [1/2] verify world.c -- 7 candidate(s)
cstatic: [2/2] verify world.h -- 1 candidate(s)

cstatic: collapsing findings that share one root cause
cstatic: no duplicates to merge

cstatic: ---------------------------------------------------------------

world.c:41:9: error: loadTitle clamps len to MAX_NAME_LEN, so the terminator at line 46 writes one past title
    title is char[16] (world.h@20, MAX_NAME_LEN 16), so title[16] at
    world.c@46 is out of bounds
    any inTitle of 16 or more chars triggers it; strncpy also fills all 16
    bytes unterminated ...

world.c:54:18: error: growItems doubles capacity, so a world made with newWorld( 0 ) never gains any room
    newCap = 0 * 2 == 0; addItem then writes items[0] into a zero-sized block
    newWorld takes inCapacity as a plain int and rejects nothing, so 0 is
    permitted
    fix: `newCap = inWorld->capacity * 2; if( newCap < 1 ) newCap = 1;`

world.c:56:22: error: realloc result is assigned over inWorld->items, losing the old block when realloc fails
    on failure items becomes NULL, capacity is still raised at world.c@58, and
    addItem
    writes through NULL at world.c@70; the old array is leaked as well
    fix: realloc into a temporary and commit both fields only on success

world.c:72:5: error: strcpy into slot->name overruns the 16-byte name field for any longer inName
    Item.name is char[MAX_NAME_LEN] == char[16] (world.h@10); a 20-char inName
    smashes slot->value and slot->count and beyond
    fix: copy at most MAX_NAME_LEN - 1 bytes and terminate explicitly

world.c:86:20: error: findItem loop bound `i <= inWorld->numItems` reads one Item past the last stored one
    with numItems == n, strcmp touches items[n], which is uninitialized or
    past the block
    a stale or garbage name there can make findItem return a bogus Item*
    fix: use `i < inWorld->numItems`

world.c:102:17: warning: value * count is computed in int and only then widened, so the product can overflow
    value 100000 and count 30000 overflow int before the assignment to long
    total
    requires an item whose value * count exceeds INT_MAX; the overflow is
    undefined ...

world.c:115:50: error: printf uses %d for inWorld->totalValue, which is declared long
    totalValue is long (world.h@19); %d fetches an int from the varargs,
    undefined
    behaviour, printing garbage on targets where long is wider than int
    fix: use %ld in the format string at world.c@114

world.h:7:21: error: macro SQUARE lacks parentheses around its parameter and its body
    SQUARE( a + b ) expands to a + b * a + b, giving a + (b*a) + b
    also 1 / SQUARE( n ) expands to 1 / n * n, and SQUARE( i++ ) increments
    twice
    fix: #define SQUARE( x ) ( ( x ) * ( x ) )

cstatic: 8 finding(s): 7 error(s), 1 warning(s).
cstatic: analyzed 1 file(s) in 93s.
cstatic: these are suggestions from a language model -- verify each one.
cstatic: this report is saved at /home/jasonrohrer/.claude/cstatic/cstatic-buggy-20260803-093702.log
