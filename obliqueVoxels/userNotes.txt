Astounding!

Next:

--The front and back view in the 3D view vs the pixel view are swapped (front in pixel shows back in 3d view).  Same for left and right.

--We need to rethink how smoothers work, because just averaging colors isn't working well enough.   Is there some way we can select a group of voxels and then say... "apply smoothing" to those, and there's a curve-of-best fit that's used for shading those pixels somehow?  For example, a voxel sphere should have sphere-like shading in the pixel view.

--The infinite light source is working great, but it is creating such harsh shadows and shading.  I'm actually having better results from a cluster of nearby light sources, but its a pain to set up.  Can we get a "size" slider for light sources, that goes down to 0 for point light sources, but can get much bigger for soft-box type effects.  This might be implemented internally as a spehere of light sources... though maybe there's some other way to compute shading from a non-point source?

--We need to be able to import a PNG (with transparency!) as a flat wall of voxels.  During import, we should click three times...  first time sets the location of the bottom left corner, next click specifies one axis, other click specifies other axis... we should see previews of these axes as translucent columns of voxels, and they should be forced to be straight lines and perpendicular to each other.  While placing the last axis, we should see a translucent preview of where the image wall of voxels will land.

--There should just be a generic "extrude" function, where you select a bunch of voxels and extrude them in some direction by some distance.

--Would also be good to extrude a selection (so you could select a weird shape, and then select a volume matching that shape going back a certain depth).

--We should be able to invert a selection (selecting everything not currently selected)
