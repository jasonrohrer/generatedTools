(Add new testing notes below.)

--- addressed 2026-07-14 ---
- Cursors reworked: every white cursor now gets an auto-generated 1px black
  halo so it reads over white voxels (select, smoother, eraser were pure white).
  Draw is now a small solid white box, Erase a hollow white box with black
  borders inside and out, eyedropper redrawn (bulb / barrel / tip), and the
  Select cursor drops its centre dot in Erase mode.
- Background image now has BG x / BG y sliders (whole render-pixel offset) plus a
  Center BG reset; the offset is saved in the .ovox BGIMAGE line.
