Nice work!


Notes:

--Whenever you reposition the cursor during playback, the change is ignored.  Playback should insta-jump to the new cursor, wherver you click.  This should work in both non-looping and looping mode.  Clicking makes the selection go away, so the playback behavior should reflect that.  Currently, in loop mode, it keeps looping the now-invisible selection.  In non-loop mode, it finishes playing the non-visible selection and then stops.

--In looping mode, dragging a new selection works perfectly, and the audio instantly starts looping over the (live and changing) new selection.  Make sure NOT to break that.

--But if we click away from the selection without dragging (during loop mode), it should switch to behaving the way no-selection loop mode works (play from cursor to end of file and loop back to cursor).

--And in non-loop mode, if there's no selection, it's very useful to insta-jump during playback while clicking around (it helps you explore the file without having to hit play over and over).


