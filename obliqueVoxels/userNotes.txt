Great work!


Still needed:

--Scribble Select (see below).

--The marquee currently sticks to the FRONT of the surface you click on.  This makes sense.  However, it should also have a depth param with a slider, similar to the box tool.  So you can click a surface and drag to select a bunch of stuff below that surface too.  And I guess another slider for how much gets selected above the marquee surface too?

--We need to be able to drage the separator between the sidebar and the view area.  Some palettes have ramps that want to all be on one line, and we need to make the sidebar wider so the palettes wrap better.

--Flat and Quick Light don't work at all if the blocks in quesiton have a ramp on them, and black is the first color in the ramp.  I guess those shading methods take the first color as the color for flat shading... should probably pick a color in the middle of the ramp instead, if there's more than one color in the ramp.... but definitely never black, if the ramp has another color besides black.

--The local near-by light sources are fine, but we should also have the option to make them... infinitely far away with parallel rays or something, like the sun.  The current local lights make a very circular halo pattern on the object, and we don't always want that look, which is like there's a desk lamp nearby.  Lights should have toggles for infinite-distance sun mode, or something like that.

--It's probably time for real file pickers, when loading, saving, exporting, etc.  These should filter by desired file extension, and hide files and folders that start with . (except up dir).


Left over from last time:

--Maybe we could also have a "scribble select" tool, that adds every draged-over voxel to the slection, and then in erase mode, it would remove voxels from the selection
