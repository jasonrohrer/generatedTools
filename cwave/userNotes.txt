Nice work!

Edits are very fast now, even in large files!  I tested it with a 114 minute file, and pasting audio was instant!


Notes:

--When Loop is checked, and you start playing without anything selected, the whole file loops from the cursor point onward (correct) but then while it's playing, if you make a selection, it is ignored for looping purpose until you hit STOP and then PLAY again.  So as long as Loop is checked, it should obey the selection as soon as it exists.


--For some reason, when I tried opening an 8kHz test file, the playhead moves in a jerky fashion.  It moves very smoothly in the 44.1 files that I've tested.  Look at GLASS.WAV in this folder as an example.


Otherwise, things are looking great in terms of functionality.


So then, the final big feature:

--We should be able to open multiple files, simultaneously, as tabs that appear at the top of the screen, below the menus.

--We should be able to copy-paste between files.

--"New" should be able to specify how many tracks, and what sample rate, and what sample format (24 bit, 16 bit, 8 bit, 32-bit float, etc... whatever WAV supports).

--Since you could, potentially, paste stuff taken from different files, the pasted samples will just be interpreted at the destination sample rate (no need for rate conversion when pasting).  And I guess... if there's a sample format conversion that needs to happen during paste, it should just happen.

--Of course, each open file should have its own marks, undo chain, pre-rendered waveform views, etc.

--Switching tabs should be instant.









