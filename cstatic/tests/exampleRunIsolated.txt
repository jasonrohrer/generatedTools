cstatic v1.0 -- 1 source file(s) in /home/jasonrohrer/checkout/generatedTools/cstatic/tests/buggy
cstatic: -f world.c -- isolation mode: each file is judged on its own, with no idea who calls it
cstatic: only that file and the headers it includes are read; the rest of the folder is not looked at
cstatic: model=opus effort=high jobs=4  (1 analysis pass(es))

cstatic: [1/1] read world.c -- 8 candidate(s)

cstatic: 8 candidate finding(s) after dedupe
cstatic: verifying (each candidate gets a fresh, skeptical reader)

cstatic: [1/2] verify world.c -- 7 candidate(s)
cstatic: [2/2] verify world.h -- 1 candidate(s)

cstatic: collapsing findings that share one root cause
cstatic: no duplicates to merge

cstatic: ---------------------------------------------------------------

world.c:41:9: error: loadTitle clamps len to MAX_NAME_LEN, so title[16] = '\0' writes one byte past the array
    loadTitle(w, "0123456789abcdef") sets len 16 and world.c@46 stores
    title[16]
    title is char[MAX_NAME_LEN], MAX_NAME_LEN is 16 (world.h@20, world.h@4)
    clamp to MAX_NAME_LEN - 1

world.c:54:18: error: growItems doubles capacity, so a world created with capacity 0 never grows
    newWorld(0) then addItem: 0 >= 0 grows to 0*2 == 0 and world.c@70 writes
    items[0]
    newWorld accepts any int and stores it verbatim at world.c@17
    use a minimum of 1, e.g. newCap = capacity ? capacity * 2 : 1

world.c:56:22: warning: realloc return stored directly over inWorld->items, losing the old block on failure
    requires the realloc to fail; items becomes NULL, the old array leaks, and
    capacity is still raised at world.c@58 so addItem derefs NULL at
    world.c@70
    assign to a temp and test it before overwriting items

world.c:72:5: error: strcpy into slot->name is unbounded and overruns for any inName of 16 chars or more
    addItem(w, "a_very_long_item_name", 1, 1) writes past name into value and
    count
    Item.name is char[MAX_NAME_LEN] == char[16] (world.h@10, world.h@4)
    copy at most MAX_NAME_LEN - 1 chars and terminate explicitly

world.c:86:19: error: findItem loops i <= numItems, so it reads and strcmps one Item past the last stored one
    with numItems == capacity, items[numItems] lies outside the block from
    world.c@15
    an accidental match there returns a pointer to unwritten memory
    the test must be i < inWorld->numItems

world.c:102:17: warning: value * count is computed in int and only widened afterwards, so long total buys nothing
    needs a product beyond INT_MAX, e.g. one item with value 100000 and count
    100000
    the int v wraps before line 103 ever adds it to total
    write total += (long)items[i].value * items[i].count

world.c:114:46: error: printf uses %d for inWorld->totalValue, which is long
    totalValue is long (world.h@19); %d with a long argument is undefined
    behaviour
    on LP64 it prints garbage and misaligns nothing else only by luck; use %ld

world.h:7:21: error: SQUARE macro parameter and body are unparenthesized, so it misexpands under any operator
    SQUARE( a + b ) expands to a + b * a + b; 4 / SQUARE( 2 ) becomes 4 / 2 *
    2 == 4
    fix: #define SQUARE( x ) ( ( x ) * ( x ) )

cstatic: 8 finding(s): 6 error(s), 2 warning(s).
cstatic: analyzed 1 file(s) in 88s.
cstatic: these are suggestions from a language model -- verify each one.
