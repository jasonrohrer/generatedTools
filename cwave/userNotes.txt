Nice work!


Notes:

--The time it takes for an edit to happen (like a fade-out) depends on the length of the file, and not just on the length of the region selected.  A tiny operation (like fading out just the end of a long file) should be fast.  I think this is because you are regenerating the whole waveform MIP whenever a change is made.  Not sure what to do here, but maybe only regenerate the part that has changed?  Or something else?  Think of a good way to handle this.  Obviously, we don't want the waveform view falling out of sync with the actual samples.

--Seams when editing are lost.... like if you remove audio from the middle, or duck it to silence, you lose where the start and end of that selection was, if you're not careful.  Audacity shows actual seams, and clips in boxes that get created and can be moved around, but this might be overkill.  Maybe marks are better?  Like... you can create marks at the current cursor, or around the current selection (with a menu command or keyboard shortcut), but also when operations happen, marks are auto-created around the affected regions (or at the cut point, if something is removed).  And then selection can just snap to those marks after that, when the end of the selection is near a mark?  (near in screen space).  Marks can be above the waveform view as little carets.  And then we should be able to click on a mark to select it (and shift-click to select multiple) and then have a menu option to delete them.  So marks just build up, after various operations are performed on selections, cutting stuff, copy-pasting, etc.  And then we can delete the ones that we no longer need.  And of course the marks should auto-move, to track the actual sample they point to, when stuff is inserted or removed before a given mark.

--Normalize should have a dB level displayed too, as you move the slider, and you should be able to type a value in the DB field.  the default should be -1.0 dB





